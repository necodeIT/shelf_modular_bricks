name: datasource-impl
description: Flutter modular datasource concrete Implementation

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.53 <0.1.0"

vars:
  module:
    type: string
    prompt: In which module should the datasource be created in?
  datasource:
    type: string
    prompt: Parent datasource
  name:
    type: string
    prompt: Implementation name
