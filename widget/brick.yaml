name: widget
description: A widget or route in the presentation layer

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.53 <0.1.0"

vars:
  module:
    type: string
    prompt: In which module should the widget be created in?
  name:
    type: string
    prompt: Sidget name
