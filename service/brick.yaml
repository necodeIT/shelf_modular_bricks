name: service
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.53 <0.1.0"

vars:
  module:
    type: string
    prompt: In which module should the service be created in?
  name:
    type: string
    prompt: Service name
