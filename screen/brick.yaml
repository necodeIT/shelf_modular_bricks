name: screen
description: A screen or route in the presentation layer

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.53 <0.1.0"

vars:
  module:
    type: string
    prompt: In which module should the screen be created in?
  name:
    type: string
    prompt: Screen name
