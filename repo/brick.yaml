name: repo
description: Repository

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.53 <0.1.0"

vars:
  name:
    type: string
    prompt: Repo name
  async:
    type: boolean
    prompt: Does this repo perform async operations?
